apiVersion: v1
kind: Service
metadata:
  name: stt-server
  namespace: voice-pipeline
spec:
  selector:
    app.kubernetes.io/name: llm
  ports:
    - name: http
      port: 8001
      targetPort: 8000
