apiVersion: apps/v1
kind: Deployment
metadata:
  name: livekit
  namespace: shwalsh
  labels:
    app.kubernetes.io/name: livekit
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: livekit
  template:
    metadata:
      labels:
        app.kubernetes.io/name: livekit
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: livekit
          image: livekit/livekit-server:latest
          args: ["--config", "/etc/livekit/config.yaml"]
          ports:
            - containerPort: 7890
              name: http
            - containerPort: 7891
              name: rtc
          resources:
            requests:
              cpu: 500m
              memory: 256Mi
            limits:
              cpu: "2"
              memory: 1Gi
          volumeMounts:
            - name: config
              mountPath: /etc/livekit
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: livekit-config
---
apiVersion: v1
kind: Service
metadata:
  name: livekit-server
  namespace: shwalsh
spec:
  selector:
    app.kubernetes.io/name: livekit
  ports:
    - name: http
      port: 7890
      targetPort: 7890
    - name: rtc
      port: 7891
      targetPort: 7891
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: livekit
  namespace: shwalsh
  annotations:
    haproxy.router.openshift.io/timeout: "3600s"
spec:
  host: livekit-shwalsh.apps.alpha.modelarch.org
  to:
    kind: Service
    name: livekit-server
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
